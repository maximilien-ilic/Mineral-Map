# Mineral-Map
Python tool that generates color-coded mineralogical maps from EDS element data. Takes 11 raw element count files (Si, Ti, Al, Fe, Mn, Mg, Ca, Na, K, O, P), normalizes them and classifies each pixel to its closest mineral phase using euclidean distance.
